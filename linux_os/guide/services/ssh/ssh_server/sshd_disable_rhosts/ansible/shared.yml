# platform = multi_platform_fedora,multi_platform_rhel,multi_platform_rhv,multi_platform_sle
# reboot = false
# strategy = restrict
# complexity = low
# disruption = low

- name: Ensure ignore rhosts
  lineinfile:
    path: /etc/ssh/sshd_config
    regexp: '\#?IgnoreRhosts.*'
    line: IgnoreRhosts yes
    state: present
  register: sshd

- name: Restart service SSHD
  service:
    name: sshd
    state: restarted
  when: sshd.changed