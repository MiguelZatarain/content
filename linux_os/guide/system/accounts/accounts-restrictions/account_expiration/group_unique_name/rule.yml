documentation_complete: true

prodtype: multi_platform_sle,sle11,sle12

title: 'Ensure All groups on the System Have Unique Names'

description: 'Change groupnames, or delete groups, so each has a unique name.'

rationale: 'Unique groupnames allow for accountability on the system.'

severity: medium

ocil_clause: 'a line is returned'

ocil: |-
    Run the following command to check for duplicate group names:
    <pre>$ sudo pwck -qr</pre>
    If there are no duplicate names, no line will be returned.
